🚀 NUSANTARA v3.1 - MIGRATION RESUME CONTEXT
Date: Wednesday, May 13, 2026
Status: IN-PROGRESS (Architectural Stabilization)

--- 🎯 GOAL ---
Standardize the codebase to "NativeAI" compliance:
1. Modular Feature Structure (features/attendance, features/school, features/staff, etc.)
2. Core Consolidation (core/data, core/di, core/ui)
3. Unified Naming (CheckIn -> Attendance, Scanner -> AttendanceScanner)
4. Removal of all legacy logic (data/, domain/, ui/ root folders)

--- ✅ COMPLETED TASKS ---
1. STUDENT MODULE: Standardized under features/student.
2. SCHOOL MODULE: Consolidated under features/school. Moved repo, remote, and local data.
3. AUTH MODULE: Upgraded to NativeAI. Abstracted Google SDK from UI to repo. Introduced AuthProfile.
4. ATTENDANCE MODULE: Renamed all CheckIn/Scanner refs to Attendance. Unified repositories.
5. CLEANUP: Deleted legacy data/, domain/, ui/, and navigation/ packages.
6. ENCODING FIX: Stripped non-ASCII characters from all .kt files to resolve KSP UTFDataFormatException.
7. SSOT ALIGNMENT: Updated AppDatabase and AppModule to use new feature-scoped DAOs.

--- ⚠️ CURRENT ROADBLOCK ---
The project is structurally clean but KSP is throwing "NonExistentClass" errors.
Likely causes:
- Hilt bindings in RepositoryModule/DataSourceModule pointing to old package names.
- Some class files might have been accidentally emptied or have incorrect 'package' lines after the mass-move.
- Dependency cycles or missing imports in the newly created folders.

--- 🛠 NEXT STEPS (FOR NEXT SESSION) ---
1. Verify package declarations: Ensure every file in core/ and features/ has the correct 'package' line matching its directory.
2. Fix RepositoryModule/DataSourceModule: Manually verify every @Binds and @Provides points to the valid, existing implementation.
3. Surgical Build: Run './gradlew compileDebugKotlin' and fix errors one by one (do not use mass sed scripts anymore).
4. Verify Attendance Flow: Test the new AttendanceScannerScreen and AttendanceHistoryScreen.

--- 🗂️ NEW STRUCTURE ---
app/src/main/java/com/azuratech/azuratime/
├── core/
│   ├── data/ (AppDatabase, Repos, local/remote)
│   ├── di/ (AppModule, DataSourceModule, RepositoryModule)
│   ├── navigation/ (Screen, NavigationRoutes)
│   ├── ui/ (Main, Root, designsystem, theme, util)
│   └── sync/ (Workers, SyncManager)
└── features/
    ├── auth/
    ├── school/
    ├── student/
    └── attendance/ (Unified Check-in logic)
