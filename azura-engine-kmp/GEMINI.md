# ⚙️ Azura Engine (KMP) Rules
1.  **Pure Logic Only**: No Android framework dependencies (`Context`, `Log`, `Room`) are allowed here.
2.  **Data Models**: Provide pure Kotlin canonical models (e.g., `Account`, `Student`) that the Android `app` module maps its Entities to/from.
